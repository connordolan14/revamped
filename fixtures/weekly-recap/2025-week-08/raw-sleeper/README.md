# Raw Sleeper Payloads

Place the exact Week 8 2025 API responses here:

- `matchups.json`
- `users.json`
- `rosters.json`
- `transactions.json`

These should remain immutable test fixtures.

The coding agent should not fetch Sleeper live during CI.

A minimized normalized fixture is already available one directory above so implementation can begin before these exact raw files are added.
